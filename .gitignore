# Terraform local state and cache
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfplan
crash.log
crash.*.log

# Override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Transient lock info files created by terraform apply
.terraform.tfstate.lock.info

# Variable files — azure/dev.tfvars is intentionally tracked (no secrets, config only)
*.tfvars
*.tfvars.json
!azure/dev.tfvars

# CLI configuration files
.terraformrc
terraform.rc

# Claude Code local config
.claude/

# macOS
.DS_Store
